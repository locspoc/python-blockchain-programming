# Miner

import socketutils


wallet_list = ['192.168.1.106']
def minerServer(my_ip, wallet_list):
    # Open Server Connection
    # Rec' 2 transactions
    # Collect into block
    # Find nonce
    # Send that block to each on wallet list
    return False
